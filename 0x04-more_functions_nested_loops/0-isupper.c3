#include "main.h"

#include <stdio.h>


/**
 * _isupper - check if a letter is upper
 * @c: the numper to be checked
 * return: 1 for upper letter or 0 for any else
 */
{
int _isupper(int c);
}
int main(void);

{
	if (c >= 65 && <= 90)
		return (1);
	else
		return (0);
}
